import React from 'react'
import StreamCoursesHub from './StreamCoursesHub'

/**
 * Level-1 entry: /admin/courses-colleges
 * Renders the shared Class-10-themed stream hub. Tabs are the streams; each
 * tab shows that stream's courses/colleges/seats inline.
 */
export default function StreamSelectorPage() {
  return <StreamCoursesHub />
}